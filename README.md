# Nexara API

NestJS modular monolith for Nexara payment orchestration.

```bash
docker compose up -d postgres
cp .env.example .env
npm install
npm run start:dev
```

API base: `http://localhost:3000/v1`
